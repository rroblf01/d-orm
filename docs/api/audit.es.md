# `dorm.contrib.audit`

Trazabilidad por campo. Decora un modelo con `@audited(fields=[...])`
para registrar cada cambio a una tabla hermana `<Name>Audit`.

## API

::: dorm.contrib.audit.audited
::: dorm.contrib.audit.audit_history
