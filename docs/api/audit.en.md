# `dorm.contrib.audit`

Per-field audit trail. Decorate a model with `@audited(fields=[...])`
to log every field-level change to a sibling `<Name>Audit` table.

## API

::: dorm.contrib.audit.audited
::: dorm.contrib.audit.audit_history
